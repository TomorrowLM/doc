const checkSelector = (node) => {
                            if (!node.dataset) return
                            const data = node.dataset.dropdown
                            if (data === 'quick-shoppinglist-dropdown') return node               
                            if (node.parentNode)  return checkSelector(node.parentNode)
                            return false
                        }    
                        const dropdown = checkSelector(e.target)
                        console.log(e.target)
                        if (dropdown) {
                            $(dropdown).next().toggle()
                        }




